"use client"

// This component initializes the push notification plugin
// It has no UI and just ensures the plugin is loaded and initialized
import "@/lib/push-notification-plugin"

export default function PushNotificationInit() {
    // This component has no UI, it just initializes the plugin when mounted
    return null
}
